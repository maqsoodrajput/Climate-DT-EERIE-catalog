metadata:
  parameters:
    LRA_PATH:
      type: str
      description: the path to the LRA
      default: /pfs/lustrep3/appl/local/climatedt/data/AQUA/LRA/climatedt-o26.1

sources:
  IFS-NEMO:
    description: IFS-NEMO model
    driver: yaml_file_cat
    args:
      path: '{{CATALOG_DIR}}/catalog/IFS-NEMO/main.yaml'
  IFS-FESOM:
    description: IFS-FESOM coupled model
    driver: yaml_file_cat
    args:
      path: '{{CATALOG_DIR}}/catalog/IFS-FESOM/main.yaml'
  ICON:
    description: ICON coupled model
    driver: yaml_file_cat
    args:
      path: '{{CATALOG_DIR}}/catalog/ICON/main.yaml'
